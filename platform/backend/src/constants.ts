/**
 * API route prefixes for various feature areas
 */
export const IDENTITY_PROVIDERS_API_PREFIX = "/api/identity-providers";

/**
 * Better Auth's providerId for email/password accounts in the account table
 * (SSO accounts carry the identity provider's id instead).
 */
export const CREDENTIAL_PROVIDER_ID = "credential";
