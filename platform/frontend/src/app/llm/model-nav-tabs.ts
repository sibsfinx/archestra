/**
 * Tabs shared by the Model Providers (`/llm/model-providers`) and Models
 * (`/llm/models`) pages so users can switch between configuring provider
 * credentials and managing the models those credentials expose.
 */
export const MODEL_NAV_TABS = [
  {
    label: "Model Providers",
    href: "/llm/model-providers",
  },
  {
    label: "Models",
    href: "/llm/models",
  },
];
